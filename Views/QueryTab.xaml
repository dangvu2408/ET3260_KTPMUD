<UserControl x:Class="ET3260_Project.Views.QueryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ET3260_Project.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Wheat">
    <Grid>
        <TextBlock Text="Tra cứu thông tin" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="10"/>
        <Border Padding="10" Margin="0 25 0 0">
            
            <Grid VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>

                <ComboBox Name="queryDropdown"
                    Grid.Column="0"
                    Height="30"
                    IsEditable="True"
                    Margin="0, 0, 10, 0"
                    VerticalAlignment="Center">
                    <ComboBoxItem Content="Tra cứu điểm trượt lở" />
                    <ComboBoxItem Content="Tra cứu điểm lũ quét" />
                    <ComboBoxItem Content="Tra cứu ổ dịch" />
                    <ComboBoxItem Content="Tra cứu chi cục thú y" />
                    <ComboBoxItem Content="Tra cứu đại lý bán thuốc thú y" />
                    <ComboBoxItem Content="Tra cứu khu tạm giữ, tiêu hủy gia súc gia cầm" />
                    <ComboBoxItem Content="Tra cứu cơ sở giết mổ gia súc, gia cầm" />
                    <ComboBoxItem Content="Tra cứu các hộ chăn nuôi nhỏ lẻ" />
                    <ComboBoxItem Content="Tra cứu thông tin tổ chức chứng nhận" />
                    <ComboBoxItem Content="Tra cứu thông tin cơ sở chế biến sản phẩm chăn nuôi" />
                    <ComboBoxItem Content="Tra cứu thông tin vùng chăn nuôi an toàn dịch bệnh" />
                </ComboBox>

                <Button Content="Tra cứu" Width="150" Height="30" Grid.Column="1"  />

            </Grid>
        </Border>
    </Grid>
</UserControl>
